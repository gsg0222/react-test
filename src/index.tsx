import UserTable from 'containers/templates/UserTable';
import { StrictMode } from 'react';
import ReactDOM from 'react-dom';

ReactDOM.render(
  <StrictMode>
    <UserTable />
  </StrictMode>,
  document.getElementById('root'),
);
